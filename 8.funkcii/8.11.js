/* 
Задача 8.11

Написать функцию, принимающую массив чисел. 
Она должна возвращать `true`, если в массиве все числа двузначные, иначе `false`.
*/

/*
function every(arr) {
    for(let i = 0; i < arr.length; i++) {
        if(arr[i] > 10) {
            return true;
        } else {
            return false;
        }
    }
}

const arr3 = [18, 95, 36, 37, 42];
const arr4 = [9, 6, 76, 3, 32]

console.log(every(arr3)); // true
console.log(every(arr4)); // false
*/